const express = require('express');
const router = express.Router();
const upload = require('../module/multer')
const userController = require('../controller/place');

router.post('/siginup');
module.exports =router;